## Bottlelow

A bottle quality checker

### Init
Launch a container:
```
docker compose up
```

Connect with:
```
docker exec -it bottlelow-app-1 sh
```

Install dependecies:
```
cd /app
pip install -r requirements.txt
```